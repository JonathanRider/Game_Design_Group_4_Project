#ifndef _CONSTANTS_H_
#define _CONSTANTS_H_

enum ComponentType {GRAPHICS, CONTROLLABLE};

enum GameState {MENU, PLAYING, PAUSED};

enum Input {UP, DOWN, LEFT, RIGHT, ITEM, USE, NEXTITEM, PREVITEM};

#endif
